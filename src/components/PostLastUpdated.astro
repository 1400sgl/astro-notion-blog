---
import { Post } from '../lib/interfaces.ts'
import { getDateStr } from '../lib/blog-helpers.ts'

export interface Props {
  post: Post
}

const { post } = Astro.props
---

<div class="post-LastUpdated">
  {post.LastUpdated ? getDateStr(post.LastUpdated) : ''}
</div>

<style>
  .post-LastUpdated {
    padding: 0 0 0.3rem;
    font-size: 0.9rem;
  }
</style>
